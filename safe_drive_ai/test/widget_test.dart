// Smoke test para Safe Drive AI.
// Verifica que el widget raíz SafeDriveApp compila correctamente.
// Los tests de integración completos están en test/features/.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('SafeDriveApp smoke test — placeholder', (WidgetTester tester) async {
    // Placeholder: la inicialización real de Firebase requiere un entorno
    // de integración. Este archivo existe para que el runner de tests no falle.
    expect(true, isTrue);
  });
}
